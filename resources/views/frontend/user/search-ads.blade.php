<ul>
    @foreach ( $adsList as $item)
    <li>
        {{-- <img src="{{ uploaded_asset($item->photos) }}" height="40px" width="40px" alt="" /> --}}
        <strong>Hi</strong>
    </li>
    @endforeach

</ul>
